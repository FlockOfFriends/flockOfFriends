// map/filter over returned array
// find value of data.images.width, check if greater than 800pixels, if true, return the url link of that object/array
// unsplash PUPPIES site copy this

// piece of state to store image array ?
// week 9 day 1

import { useState } from "react";

const FindImages = (props) => {
  const [image, setImage] = useState("")

  console.log("Props in Find Images", props);
}

export default FindImages